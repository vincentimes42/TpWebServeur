DROP TABLE IF EXISTS ELEMENT;
DROP TABLE IF EXISTS LISTE ;

CREATE TABLE LISTE(idListe UUID PRIMARY KEY, titre VARCHAR(255), description VARCHAR(255));

CREATE TABLE ELEMENT(idelem UUID PRIMARY KEY, idListe UUID, titre VARCHAR(255), description VARCHAR(255), dateCrea DATETIME, dateModif DATETIME);


 FOREIGN KEY(idListe) REFERENCES LISTE(idListe)
